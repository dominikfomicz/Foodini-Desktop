{
  "name": "foodini-desktop",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "app": "electron .",
    "start:electron": "electron .",
    "generate:electron": "cd desktop_app && ng build --base-href ./ && cd .. && electron .",
    "electron-packager": "electron-packager . foodini-desktop --overwrite --platform=win32 --arch=ia32 --icon=resources/app/src/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName='Foodini Desktop'"
  },
  "author": "Wojciech Rak",
  "license": "ISC",
  "dependencies": {
    "babel-standalone": "^6.10.3",
    "electron": "^7.1.9",
    "es6-shim": "^0.35.1",
    "electron-reload": "^1.5.0",
    "electron-window-manager": "^1.0.6"
  },
  "devDependencies": {
    "electron-packager": "^14.2.1"
  }
}
